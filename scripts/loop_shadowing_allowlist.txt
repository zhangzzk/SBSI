# Hits from scripts/sweep_loop_shadowing.py that have been reviewed and judged DELIBERATE
# loop-carried state.  Reviewed 2026-08-19 (cont.187 follow-up).
#
# Keyed by path::function::name, NOT by line number.  Line numbers drift on every edit
# above them; an allowlist that goes stale either re-hides reviewed hits or floods the
# output with false new ones, which is the usual way a static check dies.
#
# The point of this file is that the THIRTEENTH hit is visibly new.  Do not add an entry
# without reading the loop and writing down why the rebind is safe.

# Best-so-far tracker: the new selector IS the state the loop carries forward.
sbsi/flow_training.py::main::best_val

# Empirical-Bayes fixed point.  `shear = candidate` is the iteration itself; the read above
# it (`np.max(np.abs(candidate - shear))`) is the convergence test, and it is SUPPOSED to
# compare against the previous iterate.
sbsi/inference.py::infer_shear::shear

# Safeguarded likelihood fixed point.  `center = accepted_center` carries the
# likelihood-increasing iterate into the next numerical stencil; the accepted
# value was computed from that previous center and checked before this update.
sbsi/catalogue_null.py::_safeguarded_numerical_recenter::center

# Layer-width fold when stacking Linear layers: `dim` is read as the in-features of the
# layer just appended, then set to hidden_dim for the next one.  Same idiom, three files.
sbsi/measurement_model.py::__init__::dim
sbsi/selection_model.py::__init__::dim
sbsi/spline_flow.py::__init__::dim

# First-seen block key retained as the reference every later shard is checked against.
sbsi/score_inference.py::merge_block_sum_caches::blk_k

# Baseline timing captured on the first pass and compared against on later ones.
scripts/bench_score_speed.py::main::ref

# Previous-value tracker: `base` is printed as the delta against the prior grid, then
# updated to the current one.
scripts/check_quadrature.py::main::base

# Contiguous-partition cursor. Each partition must start at the previous
# partition's stop, which then becomes the required start for the next row.
scripts/combine_numerical_recenter_partitions.py::main::expected_start

# First-shard reference metadata. The first cache/proposal schema is retained
# and every later shard is checked against it before concatenation.
scripts/merge_sharded_prior_model_qmc.py::main::cache_config
scripts/merge_sharded_prior_model_qmc.py::main::target_names

# Nested-M paired diagnostics. Each rung is compared with the immediately
# preceding estimate/influence before becoming the next reference rung.
scripts/run_section5_numerical_recenter.py::_full_ladder_one_step_report::previous_estimate
scripts/run_section5_numerical_recenter.py::_full_ladder_one_step_report::previous_influence

# Validation-loss best-so-far tracker used for early stopping and checkpoint
# selection; the stored score and state are updated together on improvement.
scripts/train_detection_classifier_transition_pair.py::train_model::best_score

# Binary search over record batches.  Narrowing the bracket is the algorithm.
scripts/eval_score_response.py::case_batch_plan::hi
scripts/eval_score_response.py::case_batch_plan::lo
